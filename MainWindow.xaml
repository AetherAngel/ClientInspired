<Window x:Class="GameClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        MouseDown="TitleBar_MouseDown"
        Icon="C:\Users\Administrator\source\repos\GameClient\Assets\RiotGamesIcon.ico"
        Title="League Client" Height="600" Width="1000" Background="#1C1C1C" WindowStyle="None" ResizeMode="NoResize">
    


    <Window.Resources>
        <ControlTemplate x:Key="ButtonTemplate1"/>
    </Window.Resources>


    <Grid>
        <!-- Layout Principal -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="276*" />
            <ColumnDefinition Width="24*"/>
            <!-- Painel de Login -->
            <ColumnDefinition Width="635*" />
            <ColumnDefinition Width="51*"/>
            <ColumnDefinition Width="14*"/>
            <!-- Imagem de Fundo -->
        </Grid.ColumnDefinitions>
        
        
        <!-- Painel de Login -->
        <Grid Background="#FFFFFF" Grid.Column="0" Grid.ColumnSpan="2" AllowDrop="True">
            <StackPanel VerticalAlignment="Center" Margin="10,0,20,0">
                <!-- Icone -->
                <Image x:Name="myImage" Source="C:\Users\Administrator\source\repos\GameClient\Assets\Images\LeagueLogo.png"/>

                <!-- Campo Nome de Usuário -->
                <Grid Margin="0,0,0,10">
                    <TextBox x:Name="UsernameTextBox" Height="40" FontSize="16" Padding="10" AllowDrop="False" />
                    <TextBlock Text="Nome de Usuário" FontSize="14" Foreground="#888" Margin="10,0,0,0" 
                               VerticalAlignment="Center" IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Text, ElementName=UsernameTextBox}" Value="False">
                                        <Setter Property="Visibility" Value="Hidden" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Campo Senha -->
                <PasswordBox Height="40" Margin="0,0,0,20" FontSize="16" Padding="10" AllowDrop="False" />

                <!-- Checkbox Manter Login -->
                <CheckBox Content="Manter login" FontSize="14" Foreground="#1C1C1C" Margin="0,0,0,20" />

                <!-- Botão Entrar -->
                <Button Content="Entrar" Height="50" Background="#0078D7" Foreground="White" 
                        FontSize="16" FontWeight="Bold" HorizontalAlignment="Stretch" />

                <!-- Links para Criar Conta e Ajuda -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <TextBlock Text="Criar Conta" FontSize="14" Foreground="#0078D7" Cursor="Hand" />
                    <TextBlock Text=" | " FontSize="14" Margin="5,0" />
                    <TextBlock Text="Não consegue iniciar sessão?" FontSize="14" Foreground="#0078D7" Cursor="Hand" />
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="2" Grid.ColumnSpan="3">
            <!-- Adicionando o MP4 com MediaElement -->
            <MediaElement Name="VideoPlayer"
                      Source="c:\users\administrator\source\repos\gameclient\assets\movies\dragon trainer tristana login theme.mp4" 
                      LoadedBehavior="Play"
                      UnloadedBehavior="Manual"
                      Stretch="Fill"
                      MediaEnded="VideoPlayer_MediaEnded"
                      Volume="0.1" AllowDrop="True"    />
        </Grid>

        <Button Content="X"
        Width="24" Height="24"
        FontSize="14"
        FontWeight="Bold"
        FontStyle="Normal"
        Foreground="#FFF5ECEC"
        HorizontalAlignment="Right"
        VerticalAlignment="Top"
        Click="ExitButton_Click"
        Grid.Column="3"
        RenderTransformOrigin="1.389,0.361" Grid.ColumnSpan="2" AllowDrop="False">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Definição do fundo do botão -->
                        <Border Name="Border"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Content="{TemplateBinding Content}"/>
                        </Border>
                    </Grid>

                    <!-- Definindo os Triggers dentro do ControlTemplate -->
                    <ControlTemplate.Triggers>
                        <!-- Quando o mouse passar sobre o botão -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="Red"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="DarkRed"/>
                            <Setter TargetName="Border" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- Quando o botão for pressionado -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="DarkRed"/>
                            <Setter TargetName="Border" Property="BorderBrush" Value="Red"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>


    </Grid>
</Window>